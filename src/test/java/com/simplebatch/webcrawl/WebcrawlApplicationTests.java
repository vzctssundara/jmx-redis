package com.simplebatch.webcrawl;

import org.junit.jupiter.api.Test;
import org.mockito.Mockito;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.test.context.SpringBootTest;

import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.verify;

@SpringBootTest
class WebcrawlApplicationTests {

	@Test
	void contextLoads() {
	}

	@Test
	public void testApplicationRun(){
//		SpringApplication springApplicationMock = mock(SpringApplication.class);
//		Class<WebcrawlApplicationTests>  ct=null;
//
//		SpringApplication mockStatic = mock(SpringApplication.class);
//		Mockito.when(mockStatic.run(WebcrawlApplication.class, new String[]{})).thenReturn(null);
//
//		WebcrawlApplication.main(new String[] {});
//
//		//verify(mockStatic).run(WebcrawlApplication.class, new String[] {}).close();
	}
}
